name: finality
description: Wavy Wallet manages your entire Web3 portfolio, from DeFi to NFTs and whatever comes next.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: '>=3.5.4 <4.0.0'
  flutter: '>=3.19.0'
# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions 
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter


  flutter_svg: ^2.2.2
  equatable: ^2.0.7
  shared_preferences: ^2.5.3
  url_launcher: ^6.3.2
  get_it: ^9.0.5 # Service provider . 依赖注入。
  dio: ^5.9.0
  pretty_dio_logger: ^1.4.0
  retrofit: ^4.9.1
  logger: ^2.6.2
  crypto: ^3.0.7 # MD5、SHA、HMAC
  hex: ^0.2.0
  solana: ^0.31.2+1
  json_annotation: ^4.9.0
  copy_with_extension: ^11.0.0
  dart_extensions_methods: ^2.0.0
  rxdart: ^0.28.0
  fluttertoast: ^8.2.12
  async: ^2.13.0
  collection: ^1.19.1
  qr_flutter:
    git:
      url: https://github.com/theyakka/qr.flutter
  convert: ^3.1.2
  intl: ^0.20.2
  async_button_builder: 
    git:
      url: https://github.com/z-chu/async_button_builder.git
      ref: main
  qr_code_scanner_plus: ^2.0.10+1
  image_picker: ^1.1.2
  image: ^4.5.4
  zxing2: ^0.2.4
  decimal: ^3.2.1
  flutter_easyloading: ^3.0.5
  share_plus: ^12.0.1
  webview_flutter: ^4.13.0
  video_player: ^2.9.3
  permission_handler: ^12.0.0+1
  cached_network_image: ^3.4.1 
  get: ^4.7.2 # getX
  package_info_plus: ^8.3.0 
  modal_bottom_sheet: ^3.0.0
  back_button_interceptor: ^8.0.4
  photo_view: ^0.15.0
  dismissible_page: ^1.0.2
  easy_refresh: ^3.4.0
  extended_nested_scroll_view: ^6.2.1
  flutter_screen_lock: ^9.2.1+1
  encrypt: ^5.0.3
  uuid: ^4.5.1
  local_auth: ^2.3.0
  google_sign_in: ^7.2.0
  googleapis: ^14.0.0
  http: ^1.4.0
  app_settings: ^6.1.1
  device_info_plus: ^11.4.0
  web_socket_client: ^0.2.1
  flutter_exit_app: ^1.1.4
  extended_wrap: ^0.1.5
  flutter_udid: ^4.1.0
  image_gallery_saver_plus: ^4.0.1
  skeletonizer: ^2.0.1
  action_slider: ^0.8.0-beta.3
  flutter_boring_avatars: ^2.0.1
  kumi_popup_window: ^2.0.1
  flutter_smart_dialog: ^4.9.8+8
  synchronized: ^3.3.1
  lifecycle: ^0.10.0
  haptic_feedback: ^0.5.1+1
  flutter_swipe_action_cell: ^3.1.5
  flutter_advanced_switch: ^3.1.0
  talker_flutter: ^4.8.3
  talker_dio_logger: ^4.8.3
  floating_draggable_widget: ^2.3.0
  store_scope: ^0.0.11
  flutter_screenutil: ^5.9.3
  wakelock_plus: ^1.3.2
  bounce_tapper:
    path: packages/bounce_tapper
  jelly:
    path: packages/jelly
  screenshot: ^3.0.0
  path_provider: ^2.1.5
  flutter_cache_manager: ^3.4.1
  confetti: ^0.8.0
  animated_toggle_switch: ^0.8.5
  drift: ^2.29.0
  drift_flutter: ^0.2.8
  sqlite3_flutter_libs: ^0.5.34
  flexi_formatter:
    git:
      url: https://github.com/z-chu/flexi_formatter.git
      ref: dev
  sliver_app_bar_builder: ^1.1.0
  extended_text: ^15.0.2
  dio_cookie_manager: ^3.2.0
  cookie_jar: ^4.0.8
  app_links: ^6.4.0
  pointycastle: ^4.0.0
  turnkey_sdk_flutter:
    git:
      url: https://github.com/z-chu/dart-sdk.git
      path: packages/sdk-flutter
      ref: main 
    # path: /Users/zchu/Projects/GithubProjects/dart-sdk/packages/sdk-flutter    
  provider: ^6.1.2 # turnkey sdk 
  pinput: ^6.0.1
  email_validator: ^3.0.0
  deriv_chart: # flutter pub upgrade deriv_chart 
    git:
      url: https://github.com/z-chu/flutter-chart.git
      ref: master
    # path: /Users/zchu/Projects/GithubProjects/flutter-chart  
  flutter_inset_shadow: ^2.0.3
  solana_mobile_client: ^0.1.2
  dotted_line: ^3.2.3
  toastification: ^3.0.3

dependency_overrides:
  win32: ^5.5.4
  share_plus: 11.0.0
  source_gen: ^4.0.0
  deriv_technical_analysis: ^1.1.1
  freezed_annotation: ^3.1.0
  pointycastle: ^4.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  retrofit_generator: ^10.2.0
  json_serializable: ^6.9.5
  copy_with_extension_gen: ^11.0.0
  drift_dev: ^2.29.0
  build_runner: ^2.4.15
# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: ClashDisplay
      fonts:
        - asset: assets/fonts/ClashDisplay-Extralight.otf
          weight: 200
        - asset: assets/fonts/ClashDisplay-Light.otf
          weight: 300
        - asset: assets/fonts/ClashDisplay-Regular.otf
          weight: 400
        - asset: assets/fonts/ClashDisplay-Medium.otf
          weight: 500
        - asset: assets/fonts/ClashDisplay-Semibold.otf
          weight: 600
        - asset: assets/fonts/ClashDisplay-Bold.otf
          weight: 700
        - asset: assets/fonts/ClashDisplay-Bold.otf
          weight: 800
        - asset: assets/fonts/ClashDisplay-Bold.otf
          weight: 900
    - family: DrukWide
      fonts:
        - asset: assets/fonts/DrukWide-Bold.ttf
  


  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
  assets:
    - assets/
    - assets/svgs/
    - assets/video/
    - assets/options_chart/


flutter_intl:
  enabled: true
